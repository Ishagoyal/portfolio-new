export type Project = {
  title: string;
  description: string;
  tech: string[];
  role: string;
};

export type WorkExperience = {
  title: string;
  description: string;
  tech: string[];
  role: string;
};

export type FormData = {
  name: string;
  email: string;
  message: string;
};
